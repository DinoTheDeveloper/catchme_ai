{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: CatchMe.AI",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/main.py",
            "console": "integratedTerminal",
            "justMyCode": true,
            "env": {
                "PYTHONPATH": "${workspaceFolder}",
                "OPENCV_FFMPEG_CAPTURE_OPTIONS": "capture_device_index=0"
            }
        }
    ]
}